import { Card, CardContent, Typography } from '@mui/material';

export default function Life() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Life</Typography>
        <Typography variant="body2" color="text.secondary">
          A short glimpse into my life and background.
        </Typography>
      </CardContent>
    </Card>
  );
}
